package PracticeForLoop;
//Do While loops at least once regardless if conditions are met
//It does this by running the code and then checking condition
public class doWhileLoop {
    public static void main(String[] args) {
        //Declare variable a = 10
        int a = 10;
        //Create DoWhile loop that prints hello world
        do {
            System.out.println("Hello world");
        } //Set condition to loop variable a if a is less than 10
        while (a<10);
    }
    }
    //Hello world prints out once because once the code is run the system sees the condition is not met and ends the loop